import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import { Texto } from './src/home';

export default function App() {
  return (
      <Texto/>
  );
}
